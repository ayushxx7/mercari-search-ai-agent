name: Keep Streamlit App Alive

on:
  schedule:
    - cron: '0 0 * * *' # Run once a day at midnight
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://mercari-japan.streamlit.app/
